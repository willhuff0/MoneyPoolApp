{
  "name": "money-pool-app-monorepo",
  "private": true,
  "workspaces": [
    "server",
    "shared",
    "client"
  ],
  "scripts": {
    "build": "npm run build -w shared && npm run build -w server",
    "start": "npm run start -w server",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run start -w client"
  },
  "dependencies": {
    "expo": "^54.0.25"
  }
}
